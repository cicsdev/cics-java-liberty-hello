language: java
jdk:
- openjdk8
- openjdk11
env:
- BUILD_TOOL=gradle
- BUILD_TOOL=mvn
jobs:
  include:
  - name: Maven
    env: BUILD_TOOL=mvn
    install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
    script:  mvn test -B
